<!DOCTYPE html>
<html>
<head>
  <title>Image Gallery</title>
</head>
<body>
  <h1>Image Gallery</h1>
  <% images.forEach(function(image) { %>
    <img src="public/uploads/<%= image%>" alt="<%= image %>" />
  <% }); %>
</body>
</html>